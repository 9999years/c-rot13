#include <stdio.h>
#include <string.h>

#define NELEMS(x)  (sizeof(x) / sizeof((x)[0]))

int main( int argc, char *argv[] ) {
	//input string
	//char *input;
	printf( "%d\n", argc );
	//int i = 0;
	//for( ; i < NELEMS(argv); i++ )
	//{
		//input = strcat( input, argv[i] );
	//}
	//printf( "%s", input );
	return 0;
}
